# 更新日志

## v2.1.0 (2026-05-30)

### ✨ 新功能
- **一键自动更新安装** — 检查到新版本后自动下载 ZIP 包，解压并启动新版程序，无需手动操作
- **中央版本号管理** — 新增 `VersionInfo.cs`，所有版本引用统一管理，一处修改全局生效

### 🔧 代码优化
- **全面移除重复代码** — `ApplyLanguage()` 从 50 行精简至 28 行（消除 40+ 行重复调用）
- **移除 `SetColor` 抽象层** — 直接赋值替代 if-else 反射链，提高可读性和性能
- **移除 `_isFirstLaunch` 死代码** — 删除从未触发的"首次启动隐藏到托盘"逻辑
- **修复 HwndSource 钩子泄漏** — `MainWindow_Closed` 时正确移除 Hook
- **SafeUI 异常可视化** — 捕获的异常不再仅输出 Debug，改为写入日志面板
- **fire-and-forget 添加异常处理** — `Task.Run` 不再静默吞异常
- **移除 `App.xaml.cs` 重复自启动设置** — 消除与 `MainWindow` 的 Registry 写入冲突
- **移除退出时杀进程** — 删除 `Exit` 时 `Kill` 其他实例的危险行为
- **移除 `Helper` 工具类** — 内联直接属性访问，减少间接层

### ⚡ 性能优化
- **Lang.Get() O(1) 缓存** — 语言切换时构建扁平缓存字典，后续 `Get()` 单次查找替代原来的 2-3 次字典遍历
- **`Languages` 字典只读化** — 修复每次访问（如打开语言菜单）都创建新字典的性能问题

### 🌐 翻译
- 新增 `app_subtitle` 和 `app_version` 的动态版本号注入，支持 30+ 语言

---

## v2.0 (2026-05-10)

- 全面重构：新 UI + 系统托盘 + 数据统计 + 自动更新检测
- 多语言支持（30+ 语言）
- 真实主题切换（深色/亮色）
- 三层降级点击策略：UI Automation → 自动坐标 → 手动坐标
- GitHub Actions 自动打包发布
- 全屏退出提醒
- 快捷键 Ctrl+Shift+P 捕获坐标
